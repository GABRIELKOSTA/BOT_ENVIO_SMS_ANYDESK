É necessário alterar os campos de imagens por novas imagens com mesmo nome!

Cada celular funcionará de uma forma e modelo diferente, por isso tem que haver essa atualização...
Vale ressaltar que para funcionar, é necessário ter ANYDESK instalado no celular que vai ser usado(Claro...), mas para ter acesso no celular pelo PC, é preciso instalar o Plugin AD 1 do ANYDESK na Play Store!
E dar os devidos acessos para que funcione corretamento o programa

# Para iniciar, digite no terminal: python bot.py

Antes, baixe o necessário:
- curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
- pip install PyAutoGUI
- pip install pillow
- pip install opencv-python
